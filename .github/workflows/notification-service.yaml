name: Notification Service CI

on:
    push:
        branches: [ "main" ]
        paths:
            - 'application-microservices/notification-service/**'
    pull_request:
        branches: [ "main" ]
        paths:
            - 'application-microservices/notification-service/**'

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v4
        
        - name: Set image tag
          run: echo "IMAGE_TAG=${{ github.sha }}" >> $GITHUB_ENV
        
        - name: Build Docker image
          run: |
            cd application-microservices/notification-service
            docker build -t notification-service:${{ env.IMAGE_TAG }} .
        
        - name: Print image info (simulation of push)
          run: |
            echo "Build image: notification-service:${{ env.IMAGE_TAG }}"

        - name: Print image info (only on main)
            if github.ref == 'refs/heads/main'
          run: |
            echo "Ready to push: notification-service:${{ env.IMAGE_TAG }}"