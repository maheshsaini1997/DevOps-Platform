# name: matrix-build

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#     build:
#         runs-on: ubuntu-latest

#         strategy:
#             matrix:
#                 service:
#                     - name: user-service
#                       path: application-microservices/user-service

#                     - name: payment-service
#                       path: application-microservices/payment-service

#                     - name: notification-service
#                       path: application-microservices/notification-service

#         steps:
#         - name: Checkout code
#           uses: actions/checkout@v4

#         - name: Set image tag
#           run: echo "IMAGE_TAG=${{ github.sha }}" >> $GITHUB_ENV

#         - name: Build Docker image
#           run: |
#             cd ${{ matrix.service.path }}
#             docker build -t ${{ matrix.service.name }}:${{ env.IMAGE_TAG }} .
        
#         - name: Print image info
#           run: |
#             echo "Build image: ${{ matrix.service.name }}:${{ env.IMAGE_TAG }}"
        